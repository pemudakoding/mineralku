import React from 'react';
import {setLayout} from '@/utils/Layout';

const Home = (props) => {
    return (
        <div>
            Test
        </div>
    )
}

setLayout(Home);

export default Home;
