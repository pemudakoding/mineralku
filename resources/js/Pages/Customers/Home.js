import React from 'react';
import {setLayout} from '@/Utils/Layout';

const Home = (props) => {
    return (
        <div>
            Anjays
        </div>
    )
}

setLayout(Home);

export default Home;
